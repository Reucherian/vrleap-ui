function paint(){
    console.log("paint called");
    // var myScene = document.querySelector('a-scene');
    // // var ultimateParent = document.getElementById('ultimateParent');
    // while (myScene.firstChild) {
    //     myScene.removeChild(myScene.firstChild);
    // }
    // var cam = document.createElement('a-entity');
    // cam.setAttribute('camera','');
    // cam.setAttribute('wasd-controls','');
    // cam.setAttribute('look-controls','');
    // // cam.position = "0 1.6 0";
    // cam.setAttribute("position","0 1.6 0");

    // var lhand = document.createElement('a-entity');
    // lhand.setAttribute('leap-hand','hand: left; enablePhysics: true; rawMode:true');
    // var rhand = document.createElement('a-entity');
    // rhand.setAttribute('leap-hand','hand: right; enablePhysics: true; rawMode:true');

    // cam.appendChild(lhand);
    // cam.appendChild(rhand);
    // myScene.appendChild(cam);

    // var p = document.createElement('a-entity');
    // p.setAttribute('id','paint-parent');
    // p.setAttribute('position',"0 1.6 0");
    // myScene.appendChild(p);
}
    